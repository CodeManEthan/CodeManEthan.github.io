# Drafts

Project entries that are not ready to publish. Nothing in this folder is built
or deployed. To publish one, move the `.md` back to `src/content/projects/` and
its image back to `public/images/`.
